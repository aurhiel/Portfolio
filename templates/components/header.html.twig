<div class="app-header">
{% block nav %}
  <nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container">
      {% if app.request.attributes.get('_route') == 'home' %}
      <span class="navbar-brand">LA <span class="small">(logo TODO)</span></span>
      {% else %}
      <a class="navbar-brand" href="{{ path('home') }}">LA <span class="small">(logo TODO)</span></a>
      {% endif %}
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav ml-auto">
          <li class="nav-item">
            <a class="nav-link" href="#about">À propos</a>
          </li>
          <li class="nav-item{% if app.request.attributes.get('_route') == 'projects' %} active{% endif %}">
            <a class="nav-link" href="{{ path('projects') }}">Projets</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="#">Logs.</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="#">Contact</a>
          </li>
        </ul>
      </div>
    </div>
  </nav>
{% endblock %}
</div>
